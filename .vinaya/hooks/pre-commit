#!/usr/bin/env sh
# >>> vinaya:managed:pre-commit >>>
# Vinaya commit-time gate. Runs the deterministic checks over your staged
# diff before the commit lands.
npx --yes @attalabs/vinaya@0.19.2 check --all --diff-only --local || exit 1
# <<< vinaya:managed:pre-commit <<<
