#!/usr/bin/env sh
# >>> vinaya:managed:pre-push >>>
# Vinaya pre-push gate. Runs branch/dispatch checks before the push leaves.
npx --yes @attalabs/vinaya@0.19.2 check --all --local || exit 1
# <<< vinaya:managed:pre-push <<<
